import { ClientLocationServiceValidatorDirective } from './client-location-service-validator.directive';

describe('ClientLocationServiceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientLocationServiceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
